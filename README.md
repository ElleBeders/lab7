# Панова Анастасия БАС-2 Лабораторная №3

# Задание 1

## Задача 1

### Текст задачи

Для заданного файла возвратить true, если он не содержит нуля, и false в противном случае.

### Алгоритм решения

Вызывается метод Task1, который вызывает приватный метод класса FillFileTask1 для построчного заполнения файла рандомными числами от -50 до 50. После программа возвращается в метод Task1, который читает числа из файла, преобразует их в int и, если натыкается на 0, то возвращает false, иначе true.

### Тестирование

Скриншоты результата работы программы

<img width="618" height="580" alt="1" src="https://github.com/user-attachments/assets/2e92c6cf-212e-4300-b904-13b43826ecd1" />
<img width="625" height="904" alt="11" src="https://github.com/user-attachments/assets/500b4b32-90f1-4227-ad51-60c39aea1999" />





## Задача 2

### Текст задачи

Вычислить максимальный элемент.

### Алгоритм решения

Вызывается метод Task2, который вызывает приватный метод FillFileTask2 для заполнения текстового файла случайными числами от -100 до 100. Числа записываются по 4 штуки в строку через пробел, всего 5 строк. После программа возвращается в метод Task2, который читает файл построчно, разбивает каждую строку на части по пробелу, преобразует их в int и ищет максимальное значение, возвращая его.

### Тестирование

Скриншоты результата работы программы

<img width="636" height="223" alt="2" src="https://github.com/user-attachments/assets/7a5a1409-e50e-46b8-b6f8-ea548d2770a3" />
<img width="612" height="247" alt="22" src="https://github.com/user-attachments/assets/05a069ee-0de0-4a6f-898a-c46f5c9e0e55" />





## Задача 3

### Текст задачи

Переписать в другой файл строки, оканчивающиеся на заданный символ.

### Алгоритм решения

Вызывается метод Task3, который вызывает приватный метод FillFileTask3 для заполнения текстового файла 20 строками случайной длины (от 1 до 15 символов), состоящими из английских гласных букв. После программа возвращается в метод Task3, который читает исходный файл построчно, проверяет последний символ каждой строки и, если он совпадает с заданным символом, записывает эту строку в выходной файл.

### Тестирование

Скриншоты результата работы программы

<img width="639" height="404" alt="3" src="https://github.com/user-attachments/assets/4b14bf37-3e3e-4dba-9296-a7949a1195cd" />
<img width="594" height="402" alt="33" src="https://github.com/user-attachments/assets/caf55b96-36b3-4d49-8cb8-84b8f65932a9" />




## Задача 4

### Текст задачи

Подсчитать количество пар противоположных чисел среди компонент исходного файла.

### Алгоритм решения

Вызывается метод Task4, который вызывает приватный метод FillFileTask4 для заполнения бинарного файла 20 случайными целыми числами от -50 до 50 через BinaryWriter. После программа возвращается в метод Task4, который читает файл через BinaryReader, подсчитывает частоту каждого числа в словаре Dictionary<int, int>, затем ищет пары противоположных чисел (x и -x). Для каждой пары добавляет к результату минимальное из двух количеств вхождений. 0 игнорируется. Метод возвращает общее количество пар противоположных чисел.

### Тестирование

Скриншоты результата работы программы

<img width="619" height="272" alt="4" src="https://github.com/user-attachments/assets/e2ceb75c-bc3e-490c-952c-276871956d2f" />




## Задача 5

### Текст задачи

Информация о багаже пассажира описывается массивом, где каждый элемент содержит название единицы багажа (чемодан, сумка, коробка и т.д.) и ее массу. Дан файл, содержащий сведения о багаже нескольких пассажиров. На сколько багаж максимальной массы отличается от багажа минимальной массы?

### Алгоритм решения

Вызывается метод Task5, который вызывает приватный метод FillFileTask5 для заполнения XML-файла через сериализацию. Создаётся случайное количество пассажиров (от 3 до 8), у каждого случайное количество единиц багажа (от 1 до 5) со случайной массой (от 3 до 70 кг). После программа возвращается в метод Task5, который десериализует XML-файл обратно в список пассажиров, перебирает все единицы багажа, находит максимальную и минимальную массу и возвращает их разность.

### Тестирование

Скриншоты результата работы программы

<img width="985" height="547" alt="5" src="https://github.com/user-attachments/assets/64537f09-f4a9-4466-8051-50f49ea43fc4" />




## Задача 6

### Текст задачи

Составить программу, которая удаляет из списка L за каждым вхождением элемента E один элемент, если такой есть, и он отличен от E.

### Алгоритм решения

Вызывается метод Task6, который принимает список List<int> и число E. Метод создаёт копию исходного списка, чтобы не изменять оригинал. Затем проходит по списку с конца. Если находит элемент, равный E, и после него есть следующий элемент (не последний), и этот следующий элемент не равен E, то удаляет этот следующий элемент. Метод возвращает изменённый список. Заполнение исходного списка выполняется приватным методом FillList, который генерирует 15 случайных чисел от 1 до 10.

### Тестирование

Скриншоты результата работы программы

<img width="409" height="370" alt="6" src="https://github.com/user-attachments/assets/dc31aac4-ae06-4245-9608-614976cbd6b3" />




## Задача 7

### Текст задачи

Определить, есть ли в списке L хотя бы один элемент, который равен следующему за ним (по кругу) элементу (первый элемент считать следующим для последнего).

### Алгоритм решения

Вызывается метод Task7, который принимает связный список LinkedList<int>. Метод создаёт новый пустой список result. Проходит по всем элементам исходного списка. Для каждого элемента определяет "следующий" элемент: если текущий элемент не последний, то следующий — это current.Next, если последний, то следующий — это первый элемент списка. Если значение текущего элемента равно значению следующего, то текущий элемент добавляется в result. Метод возвращает список элементов, у которых есть равный сосед. Заполнение исходного списка выполняется методом FillLinkedList, который генерирует 15 случайных чисел от 1 до 10.

### Тестирование

Скриншоты результата работы программы

<img width="466" height="413" alt="7" src="https://github.com/user-attachments/assets/3b418fa3-b5d4-4bae-8a2c-25c23bbb1ff0" />




## Задача 8

### Текст задачи

Есть перечень стран, популярных у туристов. Определить для каждой страны, какие из них посетили все n туристов, какие — некоторые из туристов, и какие — никто из туристов

### Алгоритм решения

Вызывается метод Task8 с выходными параметрами. Сначала вызывается GenerateTouristsData, который создаёт массив всех стран (6 стран) и список туристов (от 3 до 8 человек), каждый из которых случайно посещает от 4 до 5 стран из общего списка. Затем метод создаёт allVisited как копию множества стран первого туриста, а anyVisited — тоже как копию первого туриста. Далее для всех остальных туристов: allVisited пополняется только общими странами (пересечение множеств), а anyVisited пополняется всеми странами, которые посетил хотя бы один турист (объединение множеств). noOneVisited вычисляется как все страны минус те, что посетил хотя бы один турист. Результаты возвращаются через выходные параметры.

### Тестирование

Скриншоты результата работы программы

<img width="690" height="269" alt="8" src="https://github.com/user-attachments/assets/faf770a8-515c-422d-9457-d07a5bdad142" />




## Задача 9

### Текст задачи

Файл содержит текст на русском языке. Напечатать в алфавитном порядке все глухие согласные буквы, которые не входят ровно в одно слово.

### Алгоритм решения

Вызывается метод Task9, который принимает путь к текстовому файлу. Сначала FillFileTask9 заполняет файл заданными английскими строками. Затем метод читает весь текст из файла, разбивает его на слова (по пробелам, знакам препинания и переводам строк). Приводит все слова к нижнему регистру. Для каждой глухой согласной буквы (из заданного массива {p, t, k, f, s, h, c, x}) подсчитывает, в скольких различных словах она встречается. Затем формирует множество HashSet<char> из тех согласных, которые встречаются не ровно в одном слове (то есть 0 или 2 и более раз). Метод возвращает это множество.

### Тестирование

Скриншоты результата работы программы

<img width="643" height="381" alt="9" src="https://github.com/user-attachments/assets/0b3e3390-9dc5-4f60-ace2-9195a8c94af0" />




## Задача 10

### Текст задачи

В некотором вузе абитуриенты проходят предварительное тестирование, по результатам которого могут быть допущены к сдаче вступительных экзаменов в первом потоке. Тестирование проводится по двум предметам, по каждому предмету абитуриент может набрать от 0 до 100 баллов. При этом к сдаче экзаменов в первом потоке допускаются абитуриенты, набравшие по результатам тестирования не менее 30 баллов по каждому из двух предметов. На вход программы подаются сведения о результатах предварительного тестирования. Известно, что общее количество участников тестирования не превосходит 500.

В первой строке вводится количество абитуриентов, принимавших участие в тестировании, N.

Далее следуют N строк, имеющих следующий формат:

<Фамилия><Имя><Баллы>

Здесь <Фамилия>– строка, состоящая не более чем из 20 символов;<Имя>– строка, состоящая не более чем из 15 символов;<Баллы>– строка, содержащая два целых числа, разделенных пробелом, соответствующих баллам, полученным на тестировании по каждому из двух предметов. При этом <Фамилия> и <Имя>, <Имя> и <Баллы> разделены одним пробелом.

Примеры входных строк:

Ветров Роман 68 59

Анисимова Екатерина 64 88

Напишите программу, которая будет выводить на экран фамилии и имена абитуриентов, потерпевших неудачу, то есть не допущенных к сдаче экзаменов в первом потоке. При этом фамилии должны выводиться в алфавитном порядке.

### Алгоритм решения

Вызывается метод Task10, который имеет выходные параметры: список абитуриентов и два массива с баллами. Сначала вызывается GenerateApplicantsData, который создаёт от 5 до 15 случайных абитуриентов: каждому даётся фамилия и имя из заранее заданных массивов, а также два случайных балла от 0 до 100. Метод возвращает список пар (фамилия, полное имя) и два массива баллов. Затем метод Task10 создаёт SortedDictionary<string, string> (автоматическая сортировка по фамилии). Проходит по всем абитуриентам и, если хотя бы один балл меньше 30, добавляет абитуриента в словарь (фамилия как ключ, полное имя как значение). Словарь возвращается как результат — это список недопущенных абитуриентов в алфавитном порядке.

### Тестирование

Скриншоты результата работы программы

<img width="430" height="648" alt="10" src="https://github.com/user-attachments/assets/62d34b8b-1839-4885-a9e7-bb329a8ae72c" />




